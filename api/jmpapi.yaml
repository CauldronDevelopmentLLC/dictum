version: 1.0.0

options:
  http-root: dist

title: Dictum API Docs

help: >
  This page describes the JSON over HTTP API endpoints supported by this server.

apis:
  cors:   !include cors.yaml
  auth:   !include auth.yaml
  dictum: !include dictum.yaml
